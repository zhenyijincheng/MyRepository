<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhaopin.qiaodata.kotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.HentaiJumStepsActivity" />
        <activity android:name=".view.DragCityActivity" />
        <activity android:name=".view.ScrollConlictActivity" />
        <activity android:name=".view.PageViewActivity" />
        <activity
            android:name=".view.FibonacciActivity"
            android:label="@string/title_activity_fibonacci"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>